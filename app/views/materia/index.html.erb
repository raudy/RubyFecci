<p id="notice"><%= notice %></p>

<h1>Listando Materia</h1>

<table>
  <thead>
    <tr>
      <th>Cod</th>
      <th>Nombre</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @materia.each do |materium| %>
      <tr>
        <td><%= materium.cod %></td>
        <td><%= materium.nombre %></td>
        <td><%= link_to 'Mostrar', materium %></td>
        <td><%= link_to 'Editar', edit_materium_path(materium) %></td>
        <td><%= link_to 'Eliminar', materium, method: :delete, data: { confirm: 'Esta Seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nueva Materia', new_materium_path %>
